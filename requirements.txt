# ── Core agent framework ────────────────────────────────────
langgraph==1.2.4                  # Multi-agent state graphs + ReAct loop
langchain-core==1.4.0             # Base abstractions (tools, messages, prompts)
langchain==0.3.0                  # High-level chains and agent utilities
langchain-community==0.3.0        # Community integrations (PDF loaders etc.)
 
# ── LLM client (vLLM / OpenAI-compatible) ──────────────────
openai==1.45.0                    # OpenAI-compatible client — points at vLLM endpoint
 
# ── Tools ───────────────────────────────────────────────────
langchain-tavily==0.1.6           # Tavily web search tool (LangChain integration)
pymupdf==1.27.2                   # PDF parsing (also importable as `fitz`)
huggingface-hub==1.15.0           # HuggingFace Hub model + dataset search
 
# ── MCP (Model Context Protocol) ───────────────────────────
mcp==1.27.2                       # MCP Python SDK — SQLite server + client
 
# ── API layer ───────────────────────────────────────────────
fastapi[standard]==0.115.0        # REST API + auto docs
uvicorn[standard]==0.30.6         # ASGI server
sse-starlette==2.1.3              # Server-sent events for streaming agent trace
pydantic==2.11.0                  # Request/response validation (v2)
python-multipart==0.0.9           # Required by FastAPI for form data
httpx==0.27.2                     # Async HTTP client (vLLM calls + PDF fetching)
 
# ── UI ──────────────────────────────────────────────────────
streamlit==1.45.1                 # Chat interface + reasoning trace panel
 
# ── Config & utilities ──────────────────────────────────────
python-dotenv==1.0.1              # Load .env files
xxhash==3.5.0                     # Fast hashing (LangGraph state dependency)

